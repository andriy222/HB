import { View, Text } from "react-native";
import React from "react";
import AuthBackground from "../../UI/layout/backgrounds/AuthBackground";

const index = () => {
  return <AuthBackground children={undefined}></AuthBackground>;
};

export default index;
