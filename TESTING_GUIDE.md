# Посібник з тестування системи бігу та гідратації

## Огляд змін для тестування

Для швидкого тестування всього функціоналу, параметри сесії були зменшені:

| Параметр | Оригінал | Тестовий режим |
|----------|----------|----------------|
| Тривалість інтервалу | 10 хв | **1 хв** |
| Кількість інтервалів | 42 | **6** |
| Загальна тривалість | 420 хв (7 год) | **6 хв** |
| Цільова гідратація (чол.) | 3500 мл | **50 мл** |
| Цільова гідратація (жін.) | 3000 мл | **43 мл** |
| Перший інтервал | 500 мл | **12 мл** |

## Основні сценарії тестування

### 1. Тест запуску сесії

**Мета:** Перевірити, що сесія коректно стартує і таймер працює

**Кроки:**
1. Запустіть додаток
2. Виберіть стать (чоловік/жінка)
3. Натисніть "Почати сесію"
4. Очікуваний результат:
   - ✅ Логи показують: `🏁 Session started: male/female`
   - ✅ Логи показують: `⏱️ Interval timer started`
   - ✅ Логи показують: `⏰ Next interval in 60s (interval 1/6)`
   - ✅ Логи показують: `⏰ Session will complete in 6min`
   - ✅ UI показує поточний інтервал: 0/6
   - ✅ Стамина: 300/300
   - ✅ Дистанція: 0 км

### 2. Тест переходу між інтервалами

**Мета:** Перевірити автоматичний перехід між інтервалами

**Кроки:**
1. Запустіть сесію
2. Зачекайте 1 хвилину (не додавайте води)
3. Очікуваний результат:
   - ✅ Логи: `⏱️ Auto-completed interval 0`
   - ✅ Логи: `⏰ Next interval in 60s (interval 2/6)`
   - ✅ UI показує інтервал: 1/6
   - ✅ Стамина зменшилась (штраф -40 за пропущену воду в першому інтервалі)

4. Зачекайте ще 4 хвилини (загалом 5 хвилин)
5. Очікуваний результат:
   - ✅ Інтервал: 5/6
   - ✅ Стамина продовжує зменшуватись

### 3. Тест гідратації та штрафів

**Мета:** Перевірити запис води і розрахунок штрафів

**Кроки - Оптимальна гідратація (перший інтервал):**
1. Запустіть сесію (чоловік)
2. Протягом першої хвилини додайте 12+ мл води
3. Очікуваний результат:
   - ✅ Логи: `💧 +12ml → Interval 0`
   - ✅ Після завершення інтервалу: штраф 0, стамина залишається 300

**Кроки - Часткова гідратація:**
1. Новий інтервал (будь-який після 1-го)
2. Додайте 5 мл (50% від потрібних ~7.6 мл)
3. Очікуваний результат після завершення інтервалу:
   - ✅ Штраф: -4 (25-50% дефіцит)
   - ✅ Стамина зменшилась на 4

**Кроки - Нульова гідратація (перший інтервал):**
1. Запустіть нову сесію
2. НЕ додавайте воду в перший інтервал
3. Зачекайте 1 хвилину
4. Очікуваний результат:
   - ✅ Штраф: -40 (максимальний)
   - ✅ Стамина: 260/300

### 4. Тест стану аватара

**Мета:** Перевірити зміну анімації аватара залежно від стамини

**Стани аватара:**
- **Normal** (зелений): стамина > 198 (66% від 300)
- **Tired** (жовтий): стамина 99-198 (33-66%)
- **Exhausted** (червоний): стамина < 99 (< 33%)

**Кроки:**
1. Запустіть сесію без додавання води
2. Кожен інтервал буде давати штраф
3. Очікуваний результат:
   - ✅ Інтервали 0-1: Avatar = Normal
   - ✅ Інтервали 2-3: Avatar = Tired (стамина падає нижче 198)
   - ✅ Інтервали 4-5: Avatar = Exhausted (стамина падає нижче 99)

### 5. Тест розрахунку дистанції

**Мета:** Перевірити, що дистанція розраховується правильно залежно від стамини

**Формула:**
```
Distance = (elapsedMin / duration) × maxDistance × speedRatio
speedRatio = 0.2 + 0.8 × (stamina / 300)
```

**Кроки:**
1. Запустіть сесію з оптимальною гідратацією
2. Після 3 хв (50% сесії):
   - ✅ Стамина: ~300 (якщо пили достатньо)
   - ✅ Дистанція: ~21 км (50% від 42.195 × 100% швидкість)

3. Запустіть сесію БЕЗ гідратації
4. Після 3 хв:
   - ✅ Стамина: значно нижча
   - ✅ Дистанція: менше через знижену швидкість (speedRatio < 1.0)

### 6. Тест завершення сесії

**Мета:** Перевірити автоматичне та ручне завершення

**Автоматичне завершення:**
1. Запустіть сесію
2. Зачекайте повних 6 хвилин
3. Очікуваний результат:
   - ✅ Логи: `🏁 7 hours elapsed → auto-completing session`
   - ✅ Логи: `🏁 Session complete | Distance: X.XXkm`
   - ✅ Можливо: `🏆 NEW BEST!` (якщо це найкращий результат)
   - ✅ Сесія більше не активна
   - ✅ Таймер зупинився

**Ручне завершення:**
1. Запустіть сесію
2. Натисніть "Завершити сесію" (якщо така кнопка є)
3. Очікуваний результат: такий же як автоматичне

### 7. Тест збереження/відновлення сесії

**Мета:** Перевірити, що сесія зберігається і відновлюється при перезапуску

**Кроки - Відновлення після перезапуску:**
1. Запустіть сесію
2. Додайте трохи води (наприклад, 10мл)
3. Зачекайте 2 хвилини (поточний інтервал має бути 2/6)
4. Закрийте додаток (force quit)
5. Знову відкрийте додаток
6. Очікуваний результат:
   - ✅ Логи: `💾 Restoring active session (120s elapsed, X intervals)`
   - ✅ Логи: `⏱️ Interval timer started (restored, 120s elapsed, interval 2)`
   - ✅ Поточний інтервал відновлено: 2/6
   - ✅ Стамина відновлена (має бути така сама як до закриття)
   - ✅ Дистанція відновлена
   - ✅ Історія гідратації збережена (10мл в інтервалі 0)
   - ✅ Таймер продовжує з правильного місця

**Кроки - Збереження при зміні вкладки (найважливіший тест!):**
1. Запустіть сесію
2. Додайте 10мл води
3. Зачекайте 1.5 хвилини
4. Перейдіть на іншу вкладку в браузері
5. Зачекайте 30 секунд
6. Поверніться на вкладку з додатком
7. Очікуваний результат:
   - ✅ Поточний інтервал: 2/6 (не скинувся на 0/6!)
   - ✅ Стамина така сама або трохи зменшилась (залежно від гідратації)
   - ✅ Дистанція продовжує збільшуватись
   - ✅ НЕ з'явилось повідомлення про новий старт
   - ✅ Таймер продовжує працювати з того самого місця
   - ✅ Логи показують відновлення: `⏱️ Interval timer resumed from background`

**Кроки - Збереження при згортанні додатка (мобільні):**
1. Запустіть сесію
2. Додайте воду
3. Згорніть додаток (перейдіть на home screen)
4. Зачекайте 1 хвилину
5. Поверніться до додатку
6. Очікуваний результат:
   - ✅ Сесія продовжується з того самого місця
   - ✅ Всі дані збережені
   - ✅ Інтервал не скинувся

### 8. Тест найкращого результату

**Мета:** Перевірити збереження найкращого забігу

**Кроки:**
1. Завершіть сесію з хорошими показниками (наприклад, висока стамина та дистанція)
2. Очікуваний результат:
   - ✅ Логи: `🏆 NEW BEST!`

3. Запустіть нову сесію з гіршими показниками
4. Очікуваний результат:
   - ✅ Логи НЕ показують `🏆 NEW BEST!`
   - ✅ Попередній рекорд зберігся

### 9. Тест Bluetooth (MOCK режим)

**Мета:** Перевірити роботу з BLE у режимі емуляції

**Кроки:**
1. Запустіть додаток
2. Очікуваний результат:
   - ✅ Логи: `🔧 Using MOCK BLE`

3. Імітуйте додавання води через UI
4. Очікуваний результат:
   - ✅ Вода реєструється в поточному інтервалі
   - ✅ Штрафи розраховуються правильно

## Автоматизація тестування

### Швидкий тест (30 секунд на інтервал)

Для ще швидшого тестування, можна тимчасово змінити в коді:

```typescript
// src/constants/sessionConstants.ts
export const SESSION_CONFIG = {
  duration: 3,              // 3 хвилини
  totalIntervals: 6,        // 6 інтервалів
  intervalDuration: 0.5,    // 30 секунд на інтервал
  // ...
};

export const HYDRATION_TARGETS = {
  male: {
    total: 25,              // 25 мл на всю сесію
    first10min: 6,          // 6 мл на перший інтервал
  },
  // ...
};
```

### Таблиця очікуваних штрафів

| Сценарій | Перший інтервал (0) | Звичайні інтервали (1-5) |
|----------|---------------------|--------------------------|
| 100% гідратація (12мл / 7.6мл) | 0 | 0 |
| 75-99% | -15 | -2 |
| 50-74% | -30 | -4 |
| 25-49% | -30 | -6 |
| 0-24% | -40 | -6.5 |

## Виправлені проблеми

### ✅ ВИПРАВЛЕНО: Таймер постійно перезапускається

**Було:**
- Логи показували багато разів `⏱️ Interval timer stopped` / `⏱️ Interval timer started`
- Інтервал залишався на 1/6 постійно

**Виправлення:**
- useEffect більше не залежить від колбеків start/stop
- Додана перевірка щоб не перезапускати таймер якщо він вже працює

### ✅ ВИПРАВЛЕНО: Дані скидаються при зміні вкладки

**Було:**
- При переході на іншу вкладку сесія починалася заново
- Втрачався прогрес, стамина, інтервали

**Виправлення:**
- sessionStartTimeRef більше не обнуляється при stop()
- При відновленні використовується оригінальний session.startTime
- Поточний інтервал розраховується на основі реального elapsed time

### Проблема: Дублювання SESSION_CONFIG

**Де:**
- `/home/user/HB/src/constants/sessionConstants.ts`
- `/home/user/HB/src/hooks/useBleConnection/sessionTypes.ts`

**Рекомендація:**
Імпортувати з одного джерела для уникнення розсинхронізації.

## Повернення до продакшн налаштувань

Після завершення тестування, поверніть оригінальні значення:

```typescript
export const SESSION_CONFIG = {
  maxStamina: 300,
  duration: 420,           // 7 годин
  totalIntervals: 42,
  intervalDuration: 10,    // 10 хвилин
  maxDistance: 42.195,
};

export const HYDRATION_TARGETS = {
  male: { total: 3500, first10min: 500 },
  female: { total: 3000, first10min: 500 },
};
```

## Чеклист тестування

- [ ] Сесія запускається і таймер працює
- [ ] Інтервали автоматично переходять кожну хвилину
- [ ] Гідратація записується і показується в UI
- [ ] Штрафи розраховуються правильно
- [ ] Стан аватара змінюється (normal → tired → exhausted)
- [ ] Дистанція збільшується з часом
- [ ] Швидкість зменшується при низькій стамині
- [ ] Сесія автоматично завершується через 6 хвилин
- [ ] Найкращий результат зберігається
- [ ] Сесія відновлюється після перезапуску додатка
- [ ] MOCK BLE працює коректно
- [ ] Логи показують правильні значення

---

**Примітка:** Всі часи вказані для тестового режиму (6 хв / 6 інтервалів). Для продакшн використовуйте оригінальні значення (7 год / 42 інтервали).
